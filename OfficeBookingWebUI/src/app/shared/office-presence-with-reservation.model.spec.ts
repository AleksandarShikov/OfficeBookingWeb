import { OfficePresenceWithReservation } from './office-presence-with-reservation.model';

describe('OfficePresenceWithReservation', () => {
  it('should create an instance', () => {
    expect(new OfficePresenceWithReservation()).toBeTruthy();
  });
});
