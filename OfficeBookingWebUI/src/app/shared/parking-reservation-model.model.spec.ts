import { ParkingReservationModel } from './parking-reservation-model.model';

describe('ParkingReservationModel', () => {
  it('should create an instance', () => {
    expect(new ParkingReservationModel()).toBeTruthy();
  });
});
