import { ParkingReservation } from './parking-reservation.model';

describe('ParkingReservationModel', () => {
  it('should create an instance', () => {
    expect(new ParkingReservation()).toBeTruthy();
  });
});
